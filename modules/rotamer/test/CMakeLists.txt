# Autogenerated file, run tools/build/setup_cmake.py to regenerate

include_directories(${ANN_INCLUDE_PATH}
        ${BOOST.FILESYSTEM_INCLUDE_PATH}
        ${BOOST.PROGRAMOPTIONS_INCLUDE_PATH}
        ${BOOST.RANDOM_INCLUDE_PATH}
        ${BOOST.REGEX_INCLUDE_PATH}
        ${BOOST.SYSTEM_INCLUDE_PATH}
        ${CGAL_INCLUDE_PATH}
        ${GPERFTOOLS_INCLUDE_PATH}
        ${LOG4CXX_INCLUDE_PATH}
        ${TCMALLOC_HEAPCHECKER_INCLUDE_PATH}
        ${TCMALLOC_HEAPPROFILER_INCLUDE_PATH})
link_directories(${ANN_LINK_PATH}
        ${BOOST.FILESYSTEM_LINK_PATH}
        ${BOOST.PROGRAMOPTIONS_LINK_PATH}
        ${BOOST.RANDOM_LINK_PATH}
        ${BOOST.REGEX_LINK_PATH}
        ${BOOST.SYSTEM_LINK_PATH}
        ${CGAL_LINK_PATH}
        ${GPERFTOOLS_LINK_PATH}
        ${LOG4CXX_LINK_PATH}
        ${TCMALLOC_HEAPCHECKER_LINK_PATH}
        ${TCMALLOC_HEAPPROFILER_LINK_PATH})

set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} ${ROTAMER_CXX_FLAGS}")


File(GLOB runtimepytests "${PROJECT_BINARY_DIR}/test/rotamer/test_*.py")

math(EXPR short_timeout "${IMP_TIMEOUT_FACTOR} * 5")
math(EXPR medium_timeout "${IMP_TIMEOUT_FACTOR} * 15")
math(EXPR expensive_timeout "${IMP_TIMEOUT_FACTOR} * 120")

# should make into function
foreach (test ${runtimepytests} ${PROJECT_SOURCE_DIR}/modules/rotamer/test/test_rotamer_library.py)
  GET_FILENAME_COMPONENT(name ${test} NAME_WE)
  if(EXISTS "${PROJECT_BINARY_DIR}/test/rotamer/${name}.pytests")
    FILE(READ "${PROJECT_BINARY_DIR}/test/rotamer/${name}.pytests" contents)
    STRING(REGEX REPLACE ";" "\\\\;" contents "${contents}")
    STRING(REGEX REPLACE "\n" ";" contents "${contents}")
    foreach(testline ${contents})
      string(REGEX REPLACE "([A-Za-z0-9_]+\\.[A-Za-z0-9_]+) (.*)"
                           "\\1;\\2" split "${testline}")
      list(GET split 0 methname)
      list(GET split 1 docstring)
      add_test("rotamer.${name}.${methname}" ${IMP_TEST_SETUP} python ${test} "${methname}")
      set_tests_properties("rotamer.${name}.${methname}" PROPERTIES LABELS "IMP.rotamer;test")
      set_tests_properties("rotamer.${name}.${methname}" PROPERTIES MEASUREMENT "docstring=${docstring}")
      set_tests_properties("rotamer.${name}.${methname}" PROPERTIES TIMEOUT 5)
    endforeach()
  else()
    add_test("rotamer.${name}" ${IMP_TEST_SETUP} python ${test})
    set_tests_properties("rotamer.${name}" PROPERTIES LABELS "IMP.rotamer;test")
    set_tests_properties("rotamer.${name}" PROPERTIES TIMEOUT ${short_timeout})
    set_tests_properties("rotamer.${name}" PROPERTIES COST 1)
  endif()
endforeach(test)

foreach (test )
  GET_FILENAME_COMPONENT(name ${test} NAME_WE)
  if(EXISTS "${PROJECT_BINARY_DIR}/test/rotamer/${name}.pytests")
    FILE(READ "${PROJECT_BINARY_DIR}/test/rotamer/${name}.pytests" contents)
    STRING(REGEX REPLACE ";" "\\\\;" contents "${contents}")
    STRING(REGEX REPLACE "\n" ";" contents "${contents}")
    foreach(testline ${contents})
      string(REGEX REPLACE "([A-Za-z0-9_]+\\.[A-Za-z0-9_]+) (.*)"
                           "\\1;\\2" split "${testline}")
      list(GET split 0 methname)
      list(GET split 1 docstring)
      add_test("rotamer.${name}.${methname}" ${IMP_TEST_SETUP} python ${test} "${methname}")
      set_tests_properties("rotamer.${name}.${methname}" PROPERTIES LABELS "IMP.rotamer;test")
      set_tests_properties("rotamer.${name}.${methname}" PROPERTIES MEASUREMENT "docstring=${docstring}")
      set_tests_properties("rotamer.${name}.${methname}" PROPERTIES TIMEOUT 120)
    endforeach()
  else()
    add_test("rotamer.${name}" ${IMP_TEST_SETUP} python ${test})
    set_tests_properties("rotamer.${name}" PROPERTIES LABELS "IMP.rotamer;test;medium")
    set_tests_properties("rotamer.${name}" PROPERTIES TIMEOUT ${medium_timeout})
    set_tests_properties("rotamer.${name}" PROPERTIES COST 2)
  endif()
endforeach(test)

foreach (test )
  GET_FILENAME_COMPONENT(name ${test} NAME_WE)
  if(EXISTS "${PROJECT_BINARY_DIR}/test/rotamer/${name}.pytests")
    FILE(READ "${PROJECT_BINARY_DIR}/test/rotamer/${name}.pytests" contents)
    STRING(REGEX REPLACE ";" "\\\\;" contents "${contents}")
    STRING(REGEX REPLACE "\n" ";" contents "${contents}")
    foreach(testline ${contents})
      string(REGEX REPLACE "([A-Za-z0-9_]+\\.[A-Za-z0-9_]+) (.*)"
                           "\\1;\\2" split "${testline}")
      list(GET split 0 methname)
      list(GET split 1 docstring)
      add_test("rotamer.${name}.${methname}" ${IMP_TEST_SETUP} python ${test} "${methname}")
      set_tests_properties("rotamer.${name}.${methname}" PROPERTIES LABELS "IMP.rotamer;test")
      set_tests_properties("rotamer.${name}.${methname}" PROPERTIES MEASUREMENT "docstring=${docstring}")
      set_tests_properties("rotamer.${name}.${methname}" PROPERTIES TIMEOUT 120)
    endforeach()
  else()
    add_test("rotamer.${name}" ${IMP_TEST_SETUP} python ${test})
    set_tests_properties("rotamer.${name}" PROPERTIES LABELS "IMP.rotamer;test;expensive")
    set_tests_properties("rotamer.${name}" PROPERTIES TIMEOUT ${expensive_timeout})
    set_tests_properties("rotamer.${name}" PROPERTIES COST 3)
  endif()
endforeach(test)

set(cpp_tests  )

foreach (test ${cpp_tests})
   GET_FILENAME_COMPONENT(name ${test} NAME_WE)
   add_executable("rotamer.${name}" ${test})
   target_link_libraries("rotamer.${name}"     imp_rotamer
    ${IMP_BASE_LIBRARY}
${IMP_CGAL_LIBRARY}
${IMP_ALGEBRA_LIBRARY}
${IMP_KERNEL_LIBRARY}
${IMP_DISPLAY_LIBRARY}
${IMP_SCORE_FUNCTOR_LIBRARY}
${IMP_CORE_LIBRARY}
${IMP_CONTAINER_LIBRARY}
${IMP_ATOM_LIBRARY}
    ${BOOST.FILESYSTEM_LIBRARIES}
${BOOST.PROGRAMOPTIONS_LIBRARIES}
${BOOST.SYSTEM_LIBRARIES}
${GPERFTOOLS_LIBRARIES}
${TCMALLOC_HEAPPROFILER_LIBRARIES}
${TCMALLOC_HEAPCHECKER_LIBRARIES}
${LOG4CXX_LIBRARIES}
${BOOST.RANDOM_LIBRARIES}
${CGAL_LIBRARIES}
${ANN_LIBRARIES}
${BOOST.REGEX_LIBRARIES})
   set_target_properties("rotamer.${name}" PROPERTIES
                         RUNTIME_OUTPUT_DIRECTORY "${PROJECT_BINARY_DIR}/test/rotamer/"
                         OUTPUT_NAME ${name})
   add_test("rotamer.${name}" ${IMP_TEST_SETUP}
            "${PROJECT_BINARY_DIR}/test/rotamer/${name}${CMAKE_EXECUTABLE_SUFFIX}")
   set_tests_properties("rotamer.${name}" PROPERTIES LABELS "IMP.rotamer;test")
   set_tests_properties("rotamer.${name}" PROPERTIES TIMEOUT ${short_timeout})
   set_tests_properties("rotamer.${name}" PROPERTIES COST 1)
   set(executables ${executables} "rotamer.${name}")
endforeach(test)

add_custom_target("imp_rotamer_tests" ALL DEPENDS ${executables})
