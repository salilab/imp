# Autogenerated file, run tools/build/setup_cmake.py to regenerate



imp_get_process_exit_code("setup_module kmeans" status ${PROJECT_BINARY_DIR}
                          COMMAND ${PROJECT_SOURCE_DIR}/tools/build/setup_module.py
                          --name=kmeans
                          --datapath=${IMP_DATAPATH}
                          --defines=${IMP_KMEANS_CONFIG}:
                           --source=${PROJECT_SOURCE_DIR})
if(${status} EQUAL 0)
message("Module IMP.kmeans ok")
imp_execute_process("setup_swig_wrappers kmeans" ${PROJECT_BINARY_DIR}
                    COMMAND ${PROJECT_SOURCE_DIR}/tools/build/setup_swig_wrappers.py
                          --module=kmeans
                          --datapath=${IMP_DATAPATH}
                           --source=${PROJECT_SOURCE_DIR})

if(DOXYGEN_FOUND)
# documentation
add_custom_command(OUTPUT ${PROJECT_BINARY_DIR}/doxygen/kmeans/tags ${PROJECT_BINARY_DIR}/doc/html/kmeans/index.html
   COMMAND mkdir -p doc/html
   COMMAND ${DOXYGEN_EXECUTABLE} doxygen/kmeans/Doxyfile
   COMMAND ${PROJECT_SOURCE_DIR}/tools/build/doxygen_patch_tags.py --module=kmeans --file=doxygen/kmeans/tags
   COMMAND ${PROJECT_SOURCE_DIR}/tools/build/doxygen_show_warnings.py --warn=${PROJECT_BINARY_DIR}/doxygen/kmeans/warnings.txt
   DEPENDS ${PROJECT_BINARY_DIR}/doxygen/base/tags
   WORKING_DIRECTORY ${PROJECT_BINARY_DIR}
   COMMENT "Running doxygen on kmeans")

add_custom_target("IMP.kmeans-doc" ALL DEPENDS ${PROJECT_BINARY_DIR}/doxygen/kmeans/tags)

set(IMP_DOC_DEPENDS ${IMP_DOC_DEPENDS} "IMP.kmeans-doc" CACHE INTERNAL "" FORCE)
endif(DOXYGEN_FOUND)

add_subdirectory(${PROJECT_SOURCE_DIR}/modules/kmeans/src)
add_subdirectory(${PROJECT_SOURCE_DIR}/modules/kmeans/test)
add_subdirectory(${PROJECT_SOURCE_DIR}/modules/kmeans/examples)
add_subdirectory(${PROJECT_SOURCE_DIR}/modules/kmeans/benchmark)
add_subdirectory(${PROJECT_SOURCE_DIR}/modules/kmeans/bin)
else()
if(${status} EQUAL 1)
message("Module IMP.kmeans disabled")
else()
message(FATAL_ERROR "setup_module returned ${status}")
endif()
set(IMP_KMEANS_LIBRARY CACHE INTERNAL "" FORCE)
endif()
