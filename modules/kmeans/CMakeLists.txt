# Autogenerated file, run tools/build/setup_cmake.py to regenerate



execute_process(COMMAND ${PROJECT_SOURCE_DIR}/tools/build/setup_module.py
                          --name=kmeans
                          --datapath=${IMP_DATAPATH}
                          --defines=${IMP_KMEANS_CONFIG}:
                           --source=${PROJECT_SOURCE_DIR}
                           RESULT_VARIABLE status
                           WORKING_DIRECTORY ${PROJECT_BINARY_DIR})
if(NOT ${status})
execute_process(COMMAND ${PROJECT_SOURCE_DIR}/tools/build/setup_swig_wrappers.py
                          --module=kmeans
                          --datapath=${IMP_DATAPATH}
                           --source=${PROJECT_SOURCE_DIR}
                           WORKING_DIRECTORY ${PROJECT_BINARY_DIR})
add_subdirectory(${PROJECT_SOURCE_DIR}/modules/kmeans/src)
add_subdirectory(${PROJECT_SOURCE_DIR}/modules/kmeans/test)
add_subdirectory(${PROJECT_SOURCE_DIR}/modules/kmeans/examples)
add_subdirectory(${PROJECT_SOURCE_DIR}/modules/kmeans/pyext)
add_custom_target(kmeans DEPENDS
  imp_kmeans
  imp_kmeans_wrapper
  imp_kmeans_bins
  imp_kmeans_tests
  imp_kmeans_benchmarks)
else()
message(STATUS "Module IMP.kmeans disabled")
set(IMP_KMEANS_LIBRARY CACHE INTERNAL "" FORCE)
endif()