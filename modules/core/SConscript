Import('env')

env.IMPModuleBuild(author='IMP developers', version='SVN',
                   description='General purpose features of broad utility.',
                   required_modules=['algebra'],
                   optional_dependencies=['CGAL'])
