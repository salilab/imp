/**
 *  \file ListQuadContainer.cpp   \brief A list of ParticleQuads.
 *
 *  This file is generated by a script (core/tools/make-container).
 *  Do not edit directly.
 *
 *  Copyright 2007-9 Sali Lab. All rights reserved.
 *
 */

#include "IMP/core/ListQuadContainer.h"
#include "IMP/QuadModifier.h"
#include "IMP/QuadScore.h"
#include <IMP/core/internal/quad_helpers.h>
#include <algorithm>


IMPCORE_BEGIN_NAMESPACE

namespace {
  ListQuadContainer* get_list(QuadContainer *g) {
    return dynamic_cast<ListQuadContainer*>(g);
  }
}

IMP_ACTIVE_CONTAINER_DEF(ListQuadContainer);

ListQuadContainer
::ListQuadContainer(bool):
  internal::ListLikeQuadContainer("added or removed list"){}

ListQuadContainer
::ListQuadContainer(const ParticleQuads &ps,
                         std::string name):
  internal::ListLikeQuadContainer(name)
{
  if (ps.empty()) return;
  for (unsigned int i=0; i< ps.size(); ++i) {
    IMP_USAGE_CHECK(IMP::internal::is_valid(ps[i]),
                    "Passed ParticleQuad cannot be NULL (or None)",
                    UsageException);
    IMP_USAGE_CHECK(IMP::internal::get_model(ps[i])
                    == IMP::internal::get_model(ps[0]),
                    "All particles in container must have the same model. "
                    << "Particle " << IMP::internal::get_name(ps[i])
                    << " does not.",
                    ParticleQuadException);
  }
  set_particle_quads(ps);
}

ListQuadContainer
::ListQuadContainer(std::string name):
  internal::ListLikeQuadContainer(name){
}

ListQuadContainer
::ListQuadContainer(const char *name):
  internal::ListLikeQuadContainer(name){
}

void ListQuadContainer::show(std::ostream &out) const {
  IMP_CHECK_OBJECT(this);
  out << "ListQuadContainer with " << get_number_of_particle_quads()
      << " particle_quads." << std::endl;
}



void ListQuadContainer::set_particle_quads(ParticleQuadsTemp sc) {
  if (!get_has_model() && !get_is_added_or_removed_container()
      && !sc.empty()) {
    set_model(IMP::internal::get_model(sc[0]));
  }
  update_list(sc);
}


void ListQuadContainer::clear_particle_quads() {
  ParticleQuadsTemp t;
  update_list(t);
}


void ListQuadContainer::add_particle_quad(const ParticleQuad& vt) {
  IMP_USAGE_CHECK(IMP::internal::is_valid(vt),
                  "Passed ParticleQuad cannot be NULL (or None)",
                  UsageException);

  if (!get_has_model() && !get_is_added_or_removed_container()) {
    set_model(IMP::internal::get_model(vt));
  }
  add_to_list(vt);
  IMP_USAGE_CHECK(get_is_added_or_removed_container()
                  || !get_removed_quads_container()
                  ->get_contains(vt),
                  "You cannot remove and add the same item in one time step.",
                  ParticleQuadException);
}

void
ListQuadContainer::add_particle_quads(const ParticleQuadsTemp &c) {
  if (c.empty()) return;
  if (!get_has_model() && !get_is_added_or_removed_container()) {
    set_model(IMP::internal::get_model(c[0]));
  }
  ParticleQuadsTemp cp= c;
  add_to_list(cp);
  IMP_IF_CHECK(USAGE) {
    for (unsigned int i=0; i< c.size(); ++i) {
      IMP_USAGE_CHECK(IMP::internal::is_valid(c[i]),
                    "Passed ParticleQuad cannot be NULL (or None)",
                    UsageException);
      IMP_USAGE_CHECK(get_is_added_or_removed_container()
                      || !get_removed_quads_container()
                      ->get_contains(c[i]),
            "You cannot remove and add the same item in one time step.",
                      ParticleQuadException);

    }
  }
}


ContainersTemp ListQuadContainer::get_input_containers() const {
  return ContainersTemp();
}


void ListQuadContainer::do_before_evaluate() {
  internal::ListLikeQuadContainer::do_before_evaluate();
}

void ListQuadContainer::do_after_evaluate() {
  internal::ListLikeQuadContainer::do_after_evaluate();
}

ParticlesTemp ListQuadContainer::get_state_input_particles() const {
  return ParticlesTemp();
}

ContainersTemp ListQuadContainer::get_state_input_containers() const {
  return ContainersTemp();
}

IMPCORE_END_NAMESPACE
