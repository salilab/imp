Import('env')
e = env.IMPSharedLibraryEnvironment()

files = [
         'FormFactorTable.cpp',
         'Distribution.cpp',
         'SAXSProfile.cpp',
         'SAXSScore.cpp',
        ]

# Build and install the shared library:
e.IMPSharedLibrary(files, install=True)
#e.IMPStaticLibrary(files, install=True)
