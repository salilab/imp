Import('env')

files = [
         'small_protein.pdb',
         'medium_protein.pdb',
         'large_protein.pdb',
         'huge_protein.pdb'
        ]

# Install the data files:
env.IMPModuleData(files)
