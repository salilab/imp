Import('env')


env.IMPModuleBuild(author="Daniel Russel", version="SVN",
                   description="Benchmarks",
                   required_modules=['atom', 'core', 'algebra'],
                   optional_dependencies=['CGAL'])
