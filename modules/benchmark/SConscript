Import('env')


module_env=env.IMPModuleBuild(version="SVN",
                   lib_only_required_modules=['core', 'algebra', 'rmf'],
                   optional_modules=['domino', 'atom', 'em', 'cgal'])
