Import('env')

files = [
         'Rotation3D.h',
         'Transformation3D.h',
         'geometric_alignment.h',
        ] \
        + SConscript('internal/SConscript')

# Install the include files:
env.IMPHeaders(files)
