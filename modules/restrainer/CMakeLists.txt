# Autogenerated file, run tools/build/setup_cmake.py to regenerate



imp_get_process_exit_code("setup_module restrainer" status ${PROJECT_BINARY_DIR}
                          COMMAND ${PROJECT_SOURCE_DIR}/tools/build/setup_module.py
                          --name=restrainer
                          --datapath=${IMP_DATAPATH}
                          --defines=${IMP_RESTRAINER_CONFIG}:
                           --source=${PROJECT_SOURCE_DIR})
if(${status} EQUAL 0)
message("Module IMP.restrainer ok")
imp_execute_process("setup_swig_wrappers restrainer" ${PROJECT_BINARY_DIR}
                    COMMAND ${PROJECT_SOURCE_DIR}/tools/build/setup_swig_wrappers.py
                          --module=restrainer
                          --datapath=${IMP_DATAPATH}
                           --source=${PROJECT_SOURCE_DIR})

if(DOXYGEN_FOUND)
# documentation

file(GLOB headers ${PROJECT_BINARY_DIR}/include/IMP/restrainer/*.h)
file(GLOB docs ${PROJECT_SOURCE_DIR}/modules/restrainer/doc/*.dox
               ${PROJECT_SOURCE_DIR}/modules/restrainer/doc/*.md)
file(GLOB examples ${PROJECT_BINARY_DIR}/doc/examples/restrainer/*.py
                   ${PROJECT_BINARY_DIR}/doc/examples/restrainer/*.cpp)

add_custom_command(OUTPUT ${PROJECT_BINARY_DIR}/doxygen/restrainer/tags ${PROJECT_BINARY_DIR}/doc/html/restrainer/index.html
   COMMAND mkdir -p ${PROJECT_BINARY_DIR}/doc/html
   COMMAND ln -s -f ../../include
   COMMAND ln -s -f ../../doc/examples
   COMMAND ln -s -f ../../lib
   COMMAND ${DOXYGEN_EXECUTABLE} ../../doxygen/restrainer/Doxyfile
   COMMAND ${PROJECT_SOURCE_DIR}/tools/build/doxygen_patch_tags.py --module=restrainer --file=../../doxygen/restrainer/tags
   COMMAND ${PROJECT_SOURCE_DIR}/tools/build/doxygen_show_warnings.py --warn=../../doxygen/restrainer/warnings.txt
   DEPENDS ${IMP_BASE_DOC}
${IMP_CGAL_DOC}
${IMP_ALGEBRA_DOC}
${IMP_KERNEL_DOC}
${IMP_DISPLAY_DOC}
${IMP_SCORE_FUNCTOR_DOC}
${IMP_STATISTICS_DOC}
${IMP_CORE_DOC}
${IMP_CONTAINER_DOC}
${IMP_ATOM_DOC}
${IMP_EM_DOC}
${IMP_SAXS_DOC} ${headers} ${docs} ${examples} ${PROJECT_SOURCE_DIR}/modules/restrainer/README.md
   WORKING_DIRECTORY ${PROJECT_BINARY_DIR}/doxygen/restrainer/
   COMMENT "Running doxygen on restrainer")

add_custom_target("IMP.restrainer-doc" ALL DEPENDS ${PROJECT_BINARY_DIR}/doxygen/restrainer/tags)

set(IMP_RESTRAINER_DOC "IMP.restrainer-doc" CACHE INTERNAL "" FORCE)

set(IMP_DOC_DEPENDS ${IMP_DOC_DEPENDS}
                    ${IMP_RESTRAINER_DOC} CACHE INTERNAL "" FORCE)
endif(DOXYGEN_FOUND)

add_subdirectory(${PROJECT_SOURCE_DIR}/modules/restrainer/src)
add_subdirectory(${PROJECT_SOURCE_DIR}/modules/restrainer/test)
add_subdirectory(${PROJECT_SOURCE_DIR}/modules/restrainer/examples)
add_subdirectory(${PROJECT_SOURCE_DIR}/modules/restrainer/benchmark)
add_subdirectory(${PROJECT_SOURCE_DIR}/modules/restrainer/bin)
else()

# make sure it is empty
set(IMP_RESTRAINER_DOC "" CACHE INTERNAL "" FORCE)

if(${status} EQUAL 1)
message("Module IMP.restrainer disabled")
else()
message(FATAL_ERROR "setup_module returned ${status}")
endif()
set(IMP_RESTRAINER_LIBRARY CACHE INTERNAL "" FORCE)
endif()
