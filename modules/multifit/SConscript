Import('env')

env.IMPModuleBuild(version="SVN", required_modules=['core','em','domino','atom'],
                   required_dependencies=['FFTW3','Boost.FileSystem'])
#                   required_dependencies=['Boost.FileSystem'])
#                   required_dependencies=[])
