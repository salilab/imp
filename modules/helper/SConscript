Import('env')

env.Prepend(LIBS=['imp_atom'])

# Link against CGAL if present:
if env['CGAL_LIBS']:
    env.Append(LIBS=env['CGAL_LIBS'])


env.IMPModuleBuild(author="Daniel Russel", version="SVN", description="High level helper functions built on other modules.")
