/**
 *  \file DynamicListPairContainer.cpp
 *  \brief A list of ParticlePairsTemp.
 *
 *  This file is generated by a script (internal/tools/make-container).
 *  Do not edit directly.
 *
 *  Copyright 2007-2013 IMP Inventors. All rights reserved.
 *
 */

#include "IMP/kernel/internal/InternalDynamicListPairContainer.h"
#include "IMP/kernel/PairModifier.h"
#include <IMP/base/check_macros.h>
#include <IMP/base/set.h>
#include <algorithm>


IMPKERNEL_BEGIN_INTERNAL_NAMESPACE

InternalDynamicListPairContainer
::InternalDynamicListPairContainer(Container *m,
                                        std::string name):
    P(m->get_model(), name), scope_(m) {
}


InternalDynamicListPairContainer
::InternalDynamicListPairContainer(Container *m,
                                        const char *name):
    P(m->get_model(), name), scope_(m) {
}


void InternalDynamicListPairContainer::do_show(std::ostream &out) const {
  IMP_CHECK_OBJECT(this);
  out << get_access()
      << " Pairs." << std::endl;
}
void InternalDynamicListPairContainer::add(const ParticleIndexPair& vt) {
  ParticleIndexPairs cur;
  swap(cur);
  cur.push_back(vt);
  swap(cur);
}
void InternalDynamicListPairContainer
::add(const ParticleIndexPairs &c) {
  if (c.empty()) return;
  ParticleIndexPairs cur;
  swap(cur);
  cur+=c;
  swap(cur);
}

void InternalDynamicListPairContainer::set(ParticleIndexPairs cp) {
  swap(cp);
}
void InternalDynamicListPairContainer::clear() {
  ParticleIndexPairs t;
  swap(t);
}
bool InternalDynamicListPairContainer::
check_list(const ParticleIndexes& cp) const {
  ParticleIndexes app
    = scope_->get_all_possible_indexes();

  base::set<ParticleIndex> all(app.begin(),
                                    app.end());
  for (unsigned int i=0; i< cp.size(); ++i) {
    IMP_USAGE_CHECK(all.find(cp[i]) != all.end(),
                    "Particle " << cp[i]
                    << " is not in the list of all possible particles");
  }
  return true;
}

ParticleIndexes
InternalDynamicListPairContainer::get_all_possible_indexes() const {
  return scope_->get_all_possible_indexes();
}

void InternalDynamicListPairContainer::do_before_evaluate() {
}


ParticlesTemp
InternalDynamicListPairContainer::get_input_particles() const {
  return ParticlesTemp();
}

ContainersTemp
InternalDynamicListPairContainer::get_input_containers() const {
  return ContainersTemp();
}


ParticleIndexPairs
InternalDynamicListPairContainer::get_range_indexes() const {
  return get_indexes();
}


IMPKERNEL_END_INTERNAL_NAMESPACE
