# Autogenerated file, run build/tools/setup_cmake.py to regenerate



execute_process(COMMAND ${PROJECT_SOURCE_DIR}/tools/build/setup_module.py
                          --name=atom
                          --datapath=${IMP_DATAPATH}
                          --defines=${IMP_ATOM_CONFIG}:
                           --source=${PROJECT_SOURCE_DIR}
                           RESULT_VARIABLE status
                           WORKING_DIRECTORY ${PROJECT_BINARY_DIR})
if(NOT ${status})
execute_process(COMMAND ${PROJECT_SOURCE_DIR}/tools/build/setup_swig_wrappers.py
                          --module=atom
                          --datapath=${IMP_DATAPATH}
                           --source=${PROJECT_SOURCE_DIR}
                           WORKING_DIRECTORY ${PROJECT_BINARY_DIR})
add_subdirectory(${PROJECT_SOURCE_DIR}/modules/atom/src)
add_subdirectory(${PROJECT_SOURCE_DIR}/modules/atom/test)
add_subdirectory(${PROJECT_SOURCE_DIR}/modules/atom/examples)
add_subdirectory(${PROJECT_SOURCE_DIR}/modules/atom/pyext)
add_custom_target(atom DEPENDS imp_atom atom_wrapper)
else()
message(STATUS "Module IMP.atom disabled")
set(IMP_ATOM_LIBRARY CACHE INTERNAL "" FORCE)
endif()