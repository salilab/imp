Import('env')

files = [
         'top.lib',
         'top_heav.lib',
         'par.lib',
         'par_heav.lib',
         'protein_ligand_score.lib'
        ]

# Install the data files:
env.IMPModuleData(files)
