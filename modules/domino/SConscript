Import('env')

env.IMPModuleBuild(version='nightly-22/1/2012',
                   required_modules=['display', 'algebra'],
                   optional_modules=['RMF'],
                   lib_only_required_modules=['statistics', 'core', 'container'])
