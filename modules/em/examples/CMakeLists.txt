# Autogenerated file, run tools/build/setup_cmake.py to regenerate

include_directories(${ANN_INCLUDE_PATH}
        ${BOOST.FILESYSTEM_INCLUDE_PATH}
        ${BOOST.PROGRAMOPTIONS_INCLUDE_PATH}
        ${BOOST.RANDOM_INCLUDE_PATH}
        ${BOOST.REGEX_INCLUDE_PATH}
        ${BOOST.SYSTEM_INCLUDE_PATH}
        ${CGAL_INCLUDE_PATH}
        ${GPERFTOOLS_INCLUDE_PATH}
        ${LOG4CXX_INCLUDE_PATH}
        ${TCMALLOC_HEAPCHECKER_INCLUDE_PATH}
        ${TCMALLOC_HEAPPROFILER_INCLUDE_PATH})
link_directories(${ANN_LINK_PATH}
        ${BOOST.FILESYSTEM_LINK_PATH}
        ${BOOST.PROGRAMOPTIONS_LINK_PATH}
        ${BOOST.RANDOM_LINK_PATH}
        ${BOOST.REGEX_LINK_PATH}
        ${BOOST.SYSTEM_LINK_PATH}
        ${CGAL_LINK_PATH}
        ${GPERFTOOLS_LINK_PATH}
        ${LOG4CXX_LINK_PATH}
        ${TCMALLOC_HEAPCHECKER_LINK_PATH}
        ${TCMALLOC_HEAPPROFILER_LINK_PATH})

set(pytests ${PROJECT_SOURCE_DIR}/modules/em/examples/analyze_convergence.py
${PROJECT_SOURCE_DIR}/modules/em/examples/cube.py
${PROJECT_SOURCE_DIR}/modules/em/examples/fit_restraint.py
${PROJECT_SOURCE_DIR}/modules/em/examples/generate_density_map_of_fixed_dimension.py
${PROJECT_SOURCE_DIR}/modules/em/examples/local_fitting.py
${PROJECT_SOURCE_DIR}/modules/em/examples/pdb2density.py)

math(EXPR timeout "${IMP_TIMEOUT_FACTOR} * 180")

if (${IMP_MAX_CHECKS} MATCHES "INTERNAL")
set(testarg "--test")
else()
set(testarg "")
endif()

foreach (test ${pytests})
 GET_FILENAME_COMPONENT(name ${test} NAME)
 add_test("em.${name}" ${IMP_TEST_SETUP} python ${test} ${testarg})
 set_tests_properties("em.${name}" PROPERTIES LABELS "IMP.em;example")
 set_tests_properties("em.${name}" PROPERTIES TIMEOUT ${timeout})
 set_tests_properties("em.${name}" PROPERTIES COST 3)
  #add_dependencies(${name} RMFPython)
endforeach(test)

set(cpp_tests )

foreach (test ${cpp_tests})
   GET_FILENAME_COMPONENT(name ${test} NAME)
   add_executable("em.${name}" ${test})
   target_link_libraries("em.${name}"     imp_em
    ${IMP_BASE_LIBRARY}
${IMP_CGAL_LIBRARY}
${IMP_ALGEBRA_LIBRARY}
${IMP_KERNEL_LIBRARY}
${IMP_DISPLAY_LIBRARY}
${IMP_SCORE_FUNCTOR_LIBRARY}
${IMP_STATISTICS_LIBRARY}
${IMP_CORE_LIBRARY}
${IMP_CONTAINER_LIBRARY}
${IMP_ATOM_LIBRARY}
    ${BOOST.FILESYSTEM_LIBRARIES}
${BOOST.PROGRAMOPTIONS_LIBRARIES}
${BOOST.SYSTEM_LIBRARIES}
${GPERFTOOLS_LIBRARIES}
${TCMALLOC_HEAPPROFILER_LIBRARIES}
${TCMALLOC_HEAPCHECKER_LIBRARIES}
${LOG4CXX_LIBRARIES}
${BOOST.RANDOM_LIBRARIES}
${CGAL_LIBRARIES}
${ANN_LIBRARIES}
${BOOST.REGEX_LIBRARIES})
   set_target_properties("em.${name}" PROPERTIES
                         RUNTIME_OUTPUT_DIRECTORY "${PROJECT_BINARY_DIR}/test/em/"
                         OUTPUT_NAME "${name}")
   add_test("em.${name}" ${IMP_TEST_SETUP} "${PROJECT_BINARY_DIR}/test/em/${name}${CMAKE_EXECUTABLE_SUFFIX} ${testarg}")
   set_tests_properties("em.${name}" PROPERTIES LABELS "IMP.em;example")
   set_tests_properties("em.${name}" PROPERTIES TIMEOUT ${timeout})
   set_tests_properties("em.${name}" PROPERTIES COST 3)
   set(executables ${executables} "em.${name}")
endforeach(test)

add_custom_target("imp_em_examples" ALL DEPENDS ${executables})
