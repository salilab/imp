Import('env')
e = env.IMPSharedLibraryEnvironment()

files = ['convertors.cpp',
         'EMFitRestraint.cpp',
         'IMPParticlesAccessPoint.cpp',
         'ImageEM.cpp',
         'ImageHeader.cpp',
         'VolumeEM.cpp',
         'endian.cpp'
        ]

# Build and install the shared library:
e.IMPSharedLibrary(files, install=True)
