# Autogenerated file, run build/tools/setup_cmake.py to regenerate



execute_process(COMMAND ${PROJECT_SOURCE_DIR}/tools/build/setup_module.py
                          --name=parallel
                          --datapath=${IMP_DATAPATH}
                          --defines=${IMP_PARALLEL_CONFIG}:
                           --source=${PROJECT_SOURCE_DIR}
                           RESULT_VARIABLE status
                           WORKING_DIRECTORY ${PROJECT_BINARY_DIR})
if(NOT ${status})
execute_process(COMMAND ${PROJECT_SOURCE_DIR}/tools/build/setup_swig_wrappers.py
                          --module=parallel
                          --datapath=${IMP_DATAPATH}
                           --source=${PROJECT_SOURCE_DIR}
                           WORKING_DIRECTORY ${PROJECT_BINARY_DIR})
add_subdirectory(${PROJECT_SOURCE_DIR}/modules/parallel/src)
add_subdirectory(${PROJECT_SOURCE_DIR}/modules/parallel/test)
add_subdirectory(${PROJECT_SOURCE_DIR}/modules/parallel/examples)
add_subdirectory(${PROJECT_SOURCE_DIR}/modules/parallel/pyext)
add_custom_target(parallel DEPENDS imp_parallel parallel_wrapper)
else()
message(STATUS "Module IMP.parallel disabled")
set(IMP_PARALLEL_LIBRARY CACHE INTERNAL "" FORCE)
endif()