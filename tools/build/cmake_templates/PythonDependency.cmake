# Autogenerated file, run tools/build/setup_cmake.py to regenerate
execute_process(COMMAND python -c "import %(python_module)s"
                OUTPUT_VARIABLE ignore_ouput
                ERROR_VARIABLE ignore_error
                RESULT_VARIABLE status)

if (${status})
  message(STATUS "%(pkgname)s not found")
  file(WRITE "${PROJECT_BINARY_DIR}/data/build_info/%(pkgname)s" "ok=False")
else()
  message(STATUS "%(pkgname)s found " ${%(pkgname)s_INCLUDE_DIR} " " ${%(pkgname)s_LIBRARY})
  file(WRITE "${PROJECT_BINARY_DIR}/data/build_info/%(pkgname)s" "ok=True")
endif()