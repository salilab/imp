import os
import os.path

Import('env')
penv = env.Clone()
penv.Prepend(LIBS=["imp", "imp_misc", "imp_domino", "imp_core", "imp_atom",
                   "imp_algebra"])

fs = Glob("*.cpp")

all=[]
for f in fs:
    prog= penv.Program(f.rstr())
    all.append(prog)

Return('all')
